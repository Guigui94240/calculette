pipeline {
    agent any
    stages {
        stage('Build') {
           setps{
                sh 'make build'
           }
        }
        stage('Test') {
           setps{
                sh 'make test'
           }
        }
        stage('Deploy') {
           setps{
                sh 'make deploy'
           }
        }
    }
}

